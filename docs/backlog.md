# backlog items
- buy in button still visible after not buying in
- add validatio : exactly one player must have 0 penalty points when finishing a round
- disable finish round button after clicking to prevent adding round multiple times
- when adding penalty points only show a button with the number of points. clicking adds 1. add finish and cancel
- find a way for correction when invalid scores were added